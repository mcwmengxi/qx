export interface settingType {
	sidebar: {
		opened: boolean
		withoutAnimation: boolean
		isClickCollapse: boolean
	}
	device: string
	fixedHeader: boolean
	classes: {
		hideSidebar: boolean
		openSidebar: boolean
		withoutAnimation: boolean
		mobile: boolean
	}
	hideTabs: boolean
}
